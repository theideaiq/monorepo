name: Android

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_GOOGLE_SERVICES_BASE64: ${{ secrets.ANDROID_GOOGLE_SERVICES_BASE64 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: '10'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Mobile Web
        env:
          WAYL_SECRET_KEY: "mock"
          WAYL_WEBHOOK_SECRET: "mock"
          ZAIN_SECRET_KEY: "mock"
          SUPABASE_SERVICE_ROLE_KEY: "mock"
          NEXT_PUBLIC_SUPABASE_URL: "https://mock.supabase.co"
          NEXT_PUBLIC_SITE_URL: "https://mock.site.com"
          RESEND_API_KEY: "mock"
          TELEGRAM_BOT_TOKEN: "mock"
          GEMINI_API_KEY: "mock"
          UPSTASH_REDIS_REST_URL: "https://mock.upstash.io"
          UPSTASH_REDIS_REST_TOKEN: "mock"
          WEB_APP_URL: "https://mock.app"
        run: cd apps/web && pnpm build:mobile

      - name: Restore Secrets
        run: cd apps/web && pnpm secrets:restore

      - name: Sync Capacitor
        run: cd apps/web && npx cap sync android

      - name: Build APK
        run: cd apps/web/android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v6
        with:
          name: app-debug.apk
          path: apps/web/android/app/build/outputs/apk/debug/app-debug.apk
