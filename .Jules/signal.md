## 2024-04-08 - Missing Product JSON-LD Schema
**Learning:** E-commerce product pages currently lack structured data for rich snippets, limiting the ability for search engines to display price, availability, and condition in search results.
**Action:** Always inject Schema.org/Product JSON-LD payloads using `<script type="application/ld+json">` with sanitized content to safely enable rich product cards.
