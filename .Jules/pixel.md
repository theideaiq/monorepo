## 2024-03-28 - Removed hardcoded yellow color
**Learning:** Found multiple instances of hardcoded `#facc15` color instead of using the existing `brand-yellow` design token.
**Action:** When implementing UI elements that require a yellow color, always use the `brand-yellow` design token to maintain visual consistency and adhere to the design system.
