## 2024-05-15 - Icon-only Buttons
**Learning:** Icon-only buttons without `aria-label` and `title` attributes are inaccessible to screen readers and lack visual hover tooltips for pointer users.
**Action:** Always pair the `aria-label` attribute with a `title` attribute on icon-only buttons to ensure both screen-reader accessibility and visual hover tooltips.
