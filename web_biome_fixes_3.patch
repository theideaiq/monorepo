--- apps/web/src/components/checkout/CheckoutFlow.tsx
+++ apps/web/src/components/checkout/CheckoutFlow.tsx
@@ -46,4 +46,5 @@
         >
-          <div
+          <button
+            type="button"
             className="p-6 flex items-center justify-between cursor-pointer"
             onClick={() => setStep(1)}
+            onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); setStep(1); } }}
           >
@@ -58,3 +59,3 @@
             {step > 1 && <CheckCircle2 className="w-6 h-6 text-brand-yellow" />}
-          </div>
+          </button>
           <AnimatePresence>
@@ -90,4 +91,5 @@
         >
-          <div
+          <button
+            type="button"
             className="p-6 flex items-center justify-between cursor-pointer"
             onClick={() => step >= 2 && setStep(2)}
+            onKeyDown={(e) => { if (step >= 2 && (e.key === 'Enter' || e.key === ' ')) { e.preventDefault(); setStep(2); } }}
           >
@@ -102,3 +104,3 @@
             {step > 2 && <CheckCircle2 className="w-6 h-6 text-brand-yellow" />}
-          </div>
+          </button>
           <AnimatePresence>
@@ -118,4 +120,5 @@
         >
-          <div
+          <button
+            type="button"
             className="p-6 flex items-center justify-between cursor-pointer"
             onClick={() => step === 3 && setStep(3)}
+            onKeyDown={(e) => { if (step === 3 && (e.key === 'Enter' || e.key === ' ')) { e.preventDefault(); setStep(3); } }}
           >
@@ -130,3 +133,3 @@
             </div>
-          </div>
+          </button>
           <AnimatePresence>
