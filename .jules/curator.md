## 2025-03-31 - Co-locate Tests for Workspace Packages
Structure: Tests for shared workspace packages (like `@repo/utils`) were located in consumer applications.
Rule: Tests for shared workspace packages must be co-located with their corresponding source code within the package itself, rather than being housed in consumer applications.
