## 2025-04-11 - Co-locate Tests
Structure: We group test files right next to the code they test rather than keeping them in a separate app (like `apps/web/src/lib`) or `__tests__` directory.
Rule: Test files should be co-located with their implementation files across the monorepo to follow the "Co-location" philosophy.