## 2025-03-05 - Package Test Co-location
Structure: Tests for shared workspace packages (e.g., @repo/utils) were found in consuming apps (e.g., apps/web).
Rule: Tests for shared workspace packages must be co-located alongside the module they are testing within the package's src/ directory, rather than placed in consumer applications.
