## 2024-03-07 - Co-locating workspace package tests
Structure: Tests for shared workspace packages were previously placed in consumer applications like apps/web.
Rule: Tests for shared workspace packages must be co-located alongside the module they are testing within the package's src/ directory.
