## 2024-05-23 - [Middleware Standardization]
Structure: Renamed `proxy.ts` to `middleware.ts` in both `apps/web` and `apps/admin`.
Rule: Use standard Next.js file conventions (`middleware.ts`) for global middleware instead of custom names like `proxy.ts` to improve predictability and align with framework defaults.
