## 2024-05-24 - Domain-Specific Components Co-location
Structure: Domain-specific components (e.g., ProductCard, VariantSelector) must be co-located within their feature's directory (e.g., apps/web/src/components/store/) rather than placed in generic UI folders like components/ui/.
Rule: Features should group related components rather than dumping them into a common UI folder.
