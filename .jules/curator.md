## 2024-05-22 - Feature Folder Grouping
Structure: Group components by feature (e.g., `store`, `checkout`) rather than generic type (e.g., `ui`, `components`).
Rule: Domain-specific components (e.g., `ProductCard`) must be co-located in feature folders (e.g., `apps/web/src/components/store`), not in generic `ui`.
