## 2024-05-24 - Environment schemas and .env.example files synchronization
Insight: Discrepancy between environment schema and .env.example files causes local setup failures.
Rule: Environment schemas and .env.example files must be kept in sync to avoid setup failures.
