# Scribe's Journal

## 2025-05-24 - Missing Droid Webhook Setup
Insight: The `apps/droid/README.md` mentioned "set the webhook URL" but lacked instructions or commands, leaving developers to guess or lookup Telegram API docs.
Rule: When documentation says "Do X" (like setting a webhook), explicitly provide the command (e.g., `curl`) or script to do it.
