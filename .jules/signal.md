## 2024-03-09 - Missing Product Structured Data
**Learning:** Core e-commerce entities (Products) are missing critical JSON-LD structured data which prevents Google from showing rich snippets (stars, prices, in-stock status) in search results.
**Action:** Always add `Product` schema to PDPs (Product Detail Pages) to ensure search engines can properly index prices, availability, and ratings.
